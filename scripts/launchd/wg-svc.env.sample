# Per-box secrets for polar-wg-svc. Copy to ~/wg-svc.env and
# chmod 600 — the launchd plist's wrapper sources from there.
#
# Provision the plugin row + capture the plaintext token via
# /admin-plugins.html on dock BEFORE creating this file.

# Polar_wg connection. Same Postgres instance as ideamesh, but a
# different database — wg-svc must NOT see dock's tables.
export POLAR_WG_DB_DSN="postgres://ideamesh:test123456@127.0.0.1:5432/polar_wg?sslmode=disable"

# Dock's HTTP base URL for the /internal/v1/* SDK calls.
export POLAR_DOCK_BASE="http://127.0.0.1:8080"

# Must match the plugin_modules.name row in dock.
export POLAR_PLUGIN_NAME="wg"

# Plaintext token printed ONCE by /admin-plugins.html at row creation.
# After capturing it here, the dock-side hash is the only persistent
# copy — losing this file means rotating via "Rotate key".
export POLAR_PLUGIN_TOKEN=""

# wg-svc's HTTP listen address. nginx proxies the public /v1/* +
# /api/admin/wg-* prefixes here. Keep on 127.0.0.1 — nginx is the
# only public entry.
export POLAR_WG_LISTEN="127.0.0.1:8090"

# Stamped onto plugin_modules.version + /admin-plugins.html.
export POLAR_WG_VERSION="phase1d2"

# Local data root. Bundle blobs live in $POLAR_WG_DATA_DIR/wg-bundles.
# This MUST NOT be shared with dock's UPLOAD_DIR — wg-svc owns its
# own filesystem just like it owns its own DB.
export POLAR_WG_DATA_DIR="/Users/local/wg-svc-data"

# Bearer required on GET /metrics. Re-use dock's METRICS_TOKEN so
# Prometheus can scrape both with one bearer. Empty = endpoint
# disabled (404).
export POLAR_WG_METRICS_TOKEN=""

# Needed so wg-svc can find brew-installed tools (psql etc) from
# a launchd context that doesn't inherit the operator's PATH.
export PATH="/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin"
